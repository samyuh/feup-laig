#ifdef GL_ES
precision highp float;
#endif

varying vec2 vTextureCoord;

uniform float sizeM;
uniform float sizeN;

void main() {
    /*
  vec4 color = texture2D(terrainTex, vTextureCoord);

  gl_FragColor =  color;
  */

}